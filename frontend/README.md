# Minimal README for the frontend

This is the frontend that helps developers inspect WASM, generate deploy commands, and interact with Soroban contracts.

Run locally:

npm install
npm run dev

Open http://localhost:3000
